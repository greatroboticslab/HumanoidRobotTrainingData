# Debugging

### interact.sh

interact.sh is a bash script containing example code to run to create an interact session. This allows real-time execution of commands that require the use of GPU resources.
